# MongoDB Atlas connection string. Get this from Atlas → Connect → Drivers.
# Must include a database name before the "?" (here: hrms_db).
DATABASE_URL="mongodb+srv://bchandu2385_db_user:KU6NXwChSpTsDWEi@cluster0.dektqtb.mongodb.net/hrms_db?retryWrites=true&w=majority"

# JWT secret - generate a long random string for production
JWT_SECRET="CHANGE_ME_TO_A_LONG_RANDOM_SECRET"

# Server (used only for local dev - Vercel manages its own port)
PORT=4000
NODE_ENV=development

# Allowed frontend origin (your Vercel frontend URL or custom domain)
FRONTEND_ORIGIN="https://hrms.craftytechai.in"

# Office IP whitelist for clock-in/clock-out (comma-separated, supports single
# IPs and CIDR ranges). Find your office's public IP by visiting
# https://whatismyipaddress.com from an office machine.
# Example: a single static IP, or a small range if your ISP rotates within a block.
# OFFICE_IP_WHITELIST="103.21.45.10,103.21.45.0/28"
OFFICE_IP_WHITELIST="115.98.205.167"
